# Database — Neon
DATABASE_URL=postgresql://...?sslmode=require
DATABASE_URL_UNPOOLED=postgresql://...?sslmode=require

# Auth — Better Auth
BETTER_AUTH_SECRET=                     # openssl rand -base64 32
BETTER_AUTH_URL=http://localhost:3000   # production: https://droidsmith.dev

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# AI Generation — Anthropic (NEVER prefix with NEXT_PUBLIC_)
ANTHROPIC_API_KEY=sk-ant-...

# Rate Limiting — Upstash Redis
UPSTASH_REDIS_REST_URL=https://...upstash.io
UPSTASH_REDIS_REST_TOKEN=AX...

# Payments — Stripe
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_PRO_PRICE_ID=price_...

# Free tier limit (default: 5 per month)
FREE_TIER_GENERATION_LIMIT=5
